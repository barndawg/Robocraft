// ToDo: Merge this with tips.js, this file isn't needed.

function randomIntFromInterval(min,max) {

    return Math.floor(Math.random()*(max-min+1)+min);

}

function buildTips() {

    while (true) {
        // Replace "while (true)" with "while (gamemode === build)"

    }
}

(function init() {

    // Add listener or function for when gamemode changes (Build mode to Battle)

    // Start tips function

})();
